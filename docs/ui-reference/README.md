# UI reference captures

This directory stores visual reference material that was used while shaping the Muchanipo desktop/browser UI.

- `screenshots/` — captured external/product-reference screens and Muchanipo UI snapshots.
- `snapshots/` — text/markdown notes captured alongside visual references.

These are kept out of the repository root so implementation files, docs, and reference artifacts stay easy to scan.
